const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <img className="object-cover" src="/loading.gif" alt="loading" />
    </div>
  );
};

export default Loading;
